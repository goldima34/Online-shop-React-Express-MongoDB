import React from "react";
import { Route } from "react-router-dom";

const NavBar = () => {
  return (
    <div>
      <a href="/">NavBar</a>
      <a href="/auth">Auth</a>
    </div>
  );
};

export default NavBar;
